spring:
  application:
    name: petwell-backend
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/pet_well
    username: brandon
    password: password
  jpa:
    database: postgresql
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health

token:
  secret-key: a86e7e2786d280e6b5b1f75776dff7e8a18a41cf989304cb5fb0e58522eaadca
  token-expiration: 900000

redis-cache:
  host-name: localhost
  port: 6379

logging:
  level:
    dev.brandon.petwellbackend: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
